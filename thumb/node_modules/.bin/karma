#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_karma-cli@1.0.1@karma-cli/bin/karma" "$@"
  ret=$?
else 
  node  "$basedir/../_karma-cli@1.0.1@karma-cli/bin/karma" "$@"
  ret=$?
fi
exit $ret
