#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_phantomjs@2.1.7@phantomjs/bin/phantomjs" "$@"
  ret=$?
else 
  node  "$basedir/../_phantomjs@2.1.7@phantomjs/bin/phantomjs" "$@"
  ret=$?
fi
exit $ret
